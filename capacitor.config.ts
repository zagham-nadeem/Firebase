import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aliensoft.fireBase',
  appName: 'fireBase',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
